apply plugin: 'maven'

repositories{
  jcenter()
  flatDir{
      dirs 'libs'
  }
  maven { url 'dependencies' }
  mavenCentral()
}

dependencies {
   compile(name:'android-sdk-4.7.3', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
